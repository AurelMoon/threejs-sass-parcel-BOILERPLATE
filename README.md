# Simple THREE.js boilerplate

Simple THREE.js boilerplate with SASS preprocessor and PARCEL bundler.

Install packages

    npm install

Run dev enviroment

    npm run dev

Run prduction build

    npm run build
